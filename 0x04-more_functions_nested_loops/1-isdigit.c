#include "main.h"
#include <stdio.h>

/**
 * _isdigit - entry point
 * @c: an int
 * Return: 0
 */
int _isdigit(int c)
{
	if (_isdigit(c))
	{
		_putchar(c);
		return (1);
	}
	else
	{
		_putchar(c);
		return (0);
	}
}
