#include "main.h"
#include <stdio.h>

/**
 * _isupper - entry point
 * @c:a char
 * Return: 0
 */
int _isupper(int c)
{
	if (_isupper(c)) == 1
	{
		_putchar('c');
		return (1);
	}
	else
	{
		_putchar('c');
		return (0);
	}
}
