#include "main.h"
#include <stdio.h>

/**
 * _puts - entry point
 * @str: a char
 * Return: void
 */
void _puts(char *str)
{
	int i = 0;

	while (s[i] != '\0')
	{
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}
